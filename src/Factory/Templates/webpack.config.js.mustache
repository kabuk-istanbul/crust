var path = require("path");
var webpack = require("webpack");
module.exports = {
    cache: true,
    entry: "./src/js/theme.js",
    output: {
        path: path.join(__dirname, "js"),
        publicPath: "js/",
        filename: "{{theme.slug}}.js",
        chunkFilename: "[chunkhash].js"
    }
};