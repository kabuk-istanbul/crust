<?php

get_header();

while (have_posts()):

    the_post();
    ${{postType.id}} = get_post();
{% for meta in postType.metas %}
    ${{postType.id}}_{{meta.id}} = get_post_meta($post->ID, '{{meta.id}}', true);
{% endfor %}

endwhile;

get_footer();
